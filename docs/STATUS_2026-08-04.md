# Palco Capital — Estado operacional em 04/08/2026

## Produção

- Aplicação Next.js publicada em `palco-capital.vercel.app`.
- Deployment de produção do PALCO-111 em estado `READY`.
- Landing e formulário público respondendo HTTP 200.
- Intake v2 alinhado entre interface, API e RLS.
- APIs administrativas operando em modo fail-closed enquanto faltarem segredos no Vercel.

## Segurança e banco

Concluído:

- views executivas usando `security_invoker`;
- RPCs `SECURITY DEFINER` removidas do acesso direto de usuários comuns;
- autorização administrativa centralizada em `private.is_admin()`;
- policies antigas de `Chairman` e de `service_role` removidas;
- grants mínimos no caminho ativo de crédito;
- `anon` limitado a `INSERT` validado em `public.leads`;
- policy de perfil restrita ao próprio usuário autenticado;
- `search_path` fixo nas funções do schema `public`;
- índices duplicados de `leads` removidos;
- FKs dos módulos públicos cobertas por índices.

Avisos residuais:

- função e FKs do schema `arquivo_morto_lastro`, mantido em quarentena até decisão da ADR-016;
- extensão `pg_net` instalada em `public`, cuja movimentação exige ensaio isolado;
- índices classificados como não utilizados, sem volume real suficiente para exclusão segura.

## Rotinas automáticas

Os jobs abaixo continuaram concluindo com sucesso após as migrations:

- `palco_cnpj_enriquecimento`;
- `palco_orq_consumidor`;
- `palco_orq_produtores`.

## Governança de código

- PR #18: PALCO-008 — RLS e performance.
- PR #19: PALCO-009 — `search_path` e perfil.
- PR #20: PALCO-111 — intake v2, LGPD e índices.
- migrations PALCO-109 e PALCO-110 reconciliadas no GitHub.
- gate automático de qualidade preparado no PALCO-112.

## Bloqueios externos

### Vercel

Ainda precisam ser configurados fora do conector disponível:

- `PALCO_ADMIN_API_TOKEN`;
- `SUPABASE_SERVICE_ROLE_KEY`;
- `ANTHROPIC_API_KEY`, somente para o agente CEO.

### Política de crédito

A alteração do cenário severo de stress permanece aguardando ratificação do Chairman. Não deve ser modificada silenciosamente.

### Integrações operacionais

Ainda dependem de contratação, credenciais ou definição comercial:

- provedor de assinatura eletrônica;
- conta e integração bancária de desembolso;
- canais oficiais de comunicação do Chairman;
- cadastro de funding real.

## Próxima ordem recomendada

1. ativar os segredos administrativos no Vercel;
2. validar login/painel com usuário real;
3. ratificar a política de stress;
4. reconciliar baseline completo das migrations;
5. implementar assinatura e desembolso;
6. executar operação piloto controlada.
